enum IOTypeModel{IO_INVALID=0, IO_IN_DG_GEN=1, IN_AL_GEN=101, IO_OUT_DG=201};

protocolo Configuração

N M//Quantidade de dispositivos e variáveis totais

//Segue N linhas, com a i-ésima linha sendo:
TypeModel indice/porta qtVar //(do i-ésimo dispositivo)

  //Segue qtVar linhas, com a i-ésima linha sendo:
  VarId


3 5 // Quantidade de dispositivos e variáveis totais

1 2 2//TypeModel (indice/porta) qtVar depende do dispositivo
0    //varivel 1
1    

101 0 1  
4
3           //P divisores(1<=P<=+inf)
220 350 470 //P divisores (0<=DIVi<=500)
0110        //String de valores de zonas de tamanho P+1
001         //String indicadora de dominancia de tamanho P

201 0 1
3

LdVar *a = new LdVar[N];


//ler dispostivios e variaveis...


for(int i=0;i<N;i++){
  if(a[i]==nullptr){
    a[i] = static_cast<LdVar*>(new LdVarInternal(i));
  }
}


